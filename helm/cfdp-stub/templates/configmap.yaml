
apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "cfdp-stub.fullname" . }}-config
  labels:
    app.kubernetes.io/name: {{ .Chart.Name }}
    app.kubernetes.io/instance: {{ .Release.Name }}
    app.kubernetes.io/managed-by: Helm
